
const StepButton = ({ className, ...restOfProps }) => {
    return (
        <button
            className={className}
            {...restOfProps}
        >
        </button>

    )
}

export default StepButton